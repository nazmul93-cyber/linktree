require('./bootstrap');

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();


// this functionality is asynchronous to the original action  - user show  actions 
$('.user-link').click(function (e) { 
   
    
});